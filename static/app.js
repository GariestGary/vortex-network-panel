async function copyConfig(name){const r=await fetch('/devices/'+encodeURIComponent(name)+'/config');await navigator.clipboard.writeText(await r.text());alert('Client JSON copied to clipboard.');}

const subscriptionBaseUrl=document.querySelector('meta[name="vortex-public-subscription-url"]')?.content;
function subscriptionUrl(token){if(!subscriptionBaseUrl)throw new Error('Subscription URL is unavailable');return subscriptionBaseUrl+'/s/'+encodeURIComponent(token);}
function remoteProfileLink(url,name){return 'sing-box://import-remote-profile?url='+encodeURIComponent(url)+'#'+encodeURIComponent(name);}
async function subscriptionUrlFor(name){const response=await fetch('/devices/'+encodeURIComponent(name)+'/subscription-token',{credentials:'same-origin'});if(!response.ok)throw new Error('Unavailable');const payload=await response.json();return subscriptionUrl(payload.token);}
function buttonNotice(button,text){const original=button.textContent;button.textContent=text;setTimeout(()=>button.textContent=original,1500);}
async function copySubscriptionUrl(name,button){try{await navigator.clipboard.writeText(await subscriptionUrlFor(name));buttonNotice(button,'Copied');}catch{buttonNotice(button,'Unavailable');}}
async function addToSingBox(name,button){try{const url=await subscriptionUrlFor(name);window.location.href=remoteProfileLink(url,'VORTEX '+name);}catch{buttonNotice(button,'Unavailable');}}

function normalizeDeviceName(input){input.value=input.value.toUpperCase().replace(/\s+/g,'-');}
function clientTemplateElements(){return {template:document.getElementById('client-template'),revision:document.getElementById('client-template-revision'),result:document.getElementById('client-template-result')};}
async function clientTemplateRequest(url, body){const csrf=document.querySelector('meta[name="vortex-csrf"]')?.content;const response=await fetch(url,{method:'POST',credentials:'same-origin',headers:{'X-CSRF-Token':csrf},body:new URLSearchParams(body)});let data={};try{data=await response.json();}catch{} if(!response.ok)throw new Error(data.detail||data.message||'Request failed');return data;}
async function validateClientTemplate(){const {template,result}=clientTemplateElements();try{const data=await clientTemplateRequest('/settings/client-config/validate',{template:template.value});result.textContent=data.message;result.className=data.valid?'ok':'error';}catch(error){result.textContent=error.message;result.className='error';}}
async function saveClientTemplate(){const {template,revision,result}=clientTemplateElements();if(!confirm('Save this client base config? New subscription requests will use it immediately.'))return;try{const data=await clientTemplateRequest('/settings/client-config/save',{template:template.value,expected_revision:revision.value});result.textContent=data.message;result.className=data.valid?'ok':'error';if(data.valid){revision.value=data.revision;window.setTimeout(()=>location.reload(),600);}}catch(error){result.textContent=error.message;result.className='error';}}
async function restoreClientTemplate(versionId){const {revision,result}=clientTemplateElements();if(!confirm('Restore this version? The current template will be saved as a previous version first.'))return;try{const data=await clientTemplateRequest('/settings/client-config/restore/'+encodeURIComponent(versionId),{expected_revision:revision.value});result.textContent=data.message;result.className=data.valid?'ok':'error';if(data.valid)window.setTimeout(()=>location.reload(),600);}catch(error){result.textContent=error.message;result.className='error';}}